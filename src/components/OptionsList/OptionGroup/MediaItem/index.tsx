import { FC } from "react";
import styled from "styled-components";
import tw from "twin.macro";

const MediaItem: FC = () => {
  return <MediaItemContainer></MediaItemContainer>;
};

export default MediaItem;

const MediaItemContainer = styled.div`
  ${tw`
  
  `}
`;
