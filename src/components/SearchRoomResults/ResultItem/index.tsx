import { FC } from "react";
import styled from "styled-components";
import tw from "twin.macro";

const ResultItem: FC = () => {
  return <ResultItemContainer></ResultItemContainer>;
};

export default ResultItem;

const ResultItemContainer = styled.div`
  ${tw`
  
  `}
`;
